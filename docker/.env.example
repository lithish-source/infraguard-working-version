# Docker environment for InfraGuard
# Copy to .env and adjust as needed before running `docker compose up`

# JWT secret — CHANGE THIS in production!
JWT_SECRET_KEY=change_me_in_production_a1b2c3d4e5f6

# Default admin credentials (used by seed)
DEFAULT_ADMIN_EMAIL=admin@infraguard.gov
DEFAULT_ADMIN_PASSWORD=Admin@12345
