# AI Model artifacts
This directory holds trained models (.joblib) generated by `ai/train.py`.

Run `python -m ai.train` from the project root to regenerate.

Sample images are stored in `ai/data/sample_images/`.
